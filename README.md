# Logg

1. Download package "github.com/adlmidl/logg"
2. Specify the path for storage logg:<br>
<code>func main() {
  // pathToFileLogg - path for storage logg.
  InitLogg(pathToFileLogg string)
}</code>
 